name: user_repository
description: Dart package which manages the user and game data.
version: 1.0.0

publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  fleasy: ^0.4.1
  firebase_core: ^1.4.0
  cloud_firestore: ^2.4.0
  equatable: ^2.0.3
  kt_dart: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter