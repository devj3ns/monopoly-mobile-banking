name: user_repository
description: Package which manages the authentication and user data.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cloud_firestore: ^2.5.4
  firebase_auth: ^3.1.4
  firebase_core: ^1.8.0
  google_sign_in: ^5.2.0
  equatable: ^2.0.3
  rxdart: ^0.27.2
  fleasy: ^0.6.0
  deep_pick: ^0.9.0
  kt_dart: ^0.9.1

  shared:
    path: ../shared
