name: banking_repository
description: Package which manages the banking logic and data.
version: 1.0.0

publish_to: none

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  fleasy: ^0.13.0
  firebase_core: ^1.15.0
  cloud_firestore: ^3.1.13
  equatable: ^2.0.3
  kt_dart: ^0.10.0
  deep_pick: ^0.10.0

  user_repository:
    path: ../user_repository

  shared:
    path: ../shared