name: shared
description: Shared app package.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  equatable: ^2.0.3
  provider: ^5.0.0
  rxdart: ^0.27.1
  fleasy: ^0.5.0
  cloud_firestore: ^2.5.0
