name: auth_repository
description: Dart package which manages the authentication process.
version: 1.0.0

publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  fleasy: ^0.4.1
  firebase_core: ^1.4.0
  firebase_auth: ^3.0.1
  cloud_firestore: ^2.4.0
  equatable: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter